package main

import "fmt"

func main() {
	// declaring a variable named number with type int:
	var number int = 10

	// decalring a variable pi with type float64 (deciaml number / digit)
	var pi float64 = 3.14

	// declaring a variable message with type string:
	var message string = "Learning Go."

	// decalring a variable isActive with type bool
	var isActive bool = true

	// printoing to console:
	fmt.Println(number, pi, message, isActive)
}